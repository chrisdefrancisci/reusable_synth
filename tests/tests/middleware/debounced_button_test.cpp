#include <gtest/gtest.h>

#include <reusable_synth/middleware/debounced_button.hpp>

TEST(DebouncedButtonTest, risingEdgeCallbackCalled)
{
    EXPECT_TRUE(false);
}

TEST(DebouncedButtonTest, fallingEdgeCallbackCalled)
{
    EXPECT_TRUE(false);
}

TEST(DebouncedButtonTest, risingEdgeCallbackDebounced)
{
    EXPECT_TRUE(false);
}

TEST(DebouncedButtonTest, fallingEdgeCallbackDebounced)
{
    EXPECT_TRUE(false);
}

TEST(DebouncedButtonTest, getTimeSinceEdge)
{
    EXPECT_TRUE(false);
}